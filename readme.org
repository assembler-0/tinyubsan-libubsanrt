* tinyUBSan (libubsanrt)
tinyUBSan is a tiny libUBSan implementation for freestanding targets

** Usage
it is recommended to either add =tinyubsan.c= to your build system, or compile it as a dynamic/static library and link against your desired target
all the struct definitions and hander prototpes are in =tinyubsan.h=.
